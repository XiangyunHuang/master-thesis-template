--- 
site: bookdown::bookdown_site
documentclass: ctexbook
bibliography: [book.bib, packages.bib, articles.bib]
biblio-style: thuthesis-numeric
geometry: [a4paper, tmargin=3.0cm, bmargin=3.0cm, lmargin=3.0cm, rmargin=3.0cm]
link-citations: yes
natbiboptions: super,square,sort,compress
description: "This is a minimal example of using the bookdown package to write a book. The output format for this example is bookdown::gitbook."
---

```{r setup,include=FALSE}
knitr::opts_chunk$set(collapse = TRUE, fig.showtext = TRUE)
```


# 模板使用说明

本模板基于 bookdown 制作，得益于 pandoc^[<https://www.pandoc.org>]，有关如何在线发布书籍，请看文献 [@bookdown2016CRC] 第六章，以及 [@blogdown2017CRC] 第三章。

- 设置文武线
- 章节样式
- 参考文献位置
- 无序号的章节


在 `latex/before_body`添加

- 封面 
- 独创性声明
- 中英文摘要

或者目录前的部分单独制作成pdf，因为它们与后面的内容毫无关系，固定下来后，作为pdf文件合并

在 `latex/after_body` 添加

- 致谢
- 作者介绍

也可以同上的做法

参考文献标准 [GB/T 7714-2015](https://github.com/Haixing-Hu/GBT7714-2005-BibTeX-Style/files/153951/GBT.7714-2015.pdf)

## 插图

```{r}
plot(iris[,-5], col = iris$Species, main = '鸢尾花数据')
```


## 软件信息

```{r}
sessionInfo()
```


安装 tinytex 包

```r
devtools::install_github('yihui/tinytex')
tinytex::install_tinytex(TRUE, dir = '~/TinyTeX', 
    repository = 'http://mirrors.tuna.tsinghua.edu.cn/CTAN/systems/texlive/tlnet' )
```

在 `.Rprofile` 文件下设置路径

```
touch .Rprofile
```

```r
Sys.setenv(PATH = "/home/cloud2016/TinyTeX/bin/x86_64-linux:
                   /usr/local/sbin:/usr/local/bin:
                   /usr/sbin:/usr/bin:/root/bin" )
```

安装常用的 TeX 包

```bash
tlmgr install fontspec ctex tex inconsolata \
      cjk zhnumber fandol xecjk environ ulem \
      trimspaces sourcesanspro sourcecodepro \
      appendix metalogo realscripts xltxtra ms \
      biblatex biblatex-gb7714-2015 logreq \
      xstring
```

